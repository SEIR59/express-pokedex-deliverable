<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokedex</title>
    <style>
        body{
    background-color: tan;
}

    </style>
  </head>
<body>

    <h1>Welcome to the Pokemon Center!</h1>
    <a href="/pokemon/new"><button>Create New Pokemon</button></a>
    {% for poke in data %}
        <div class="container">
        <li>
            <div><a href="/pokemon/{{forloop.index0}}">{{ poke.name }}
            </a> <img src="{{poke.img}}"> <form action ='/pokemon/{{ forloop.index0 }}?_method=DELETE'
            method="POST">
                <input type="submit" value="delete">  
            </form></div>
            
        </li>
        </div>
        {% endfor %}
</body>
</html>