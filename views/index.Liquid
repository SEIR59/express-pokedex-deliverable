<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokedex</title>
</head>

<body>
    <h1>Pokedex</h1>
    <a href="/pokemon/new"><button>Add New Pokemon</button></a><br>
    {% for entry in data %}
    {% assign i = entry %}

    <a href="/pokemon/{{ forloop.index0 }}">
        <img src="{{ entry.img }}" alt="{{ entry.name }}">
    </a>

    {% endfor %}
</body>

</html>