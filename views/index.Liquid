<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <title>Document</title>
  </head>
  <body>
    <div class="navBar">
      <h1>Pokemon Manager</h1>
      <form action="/pokemon/new">
        <input type="submit" value="Create a New Pokemon" class="navInput" />
      </form>
    </div>

    <div class="flexBox">
      <ul>
        {% for pokemon in allPokemons %}
        <li>
          <div>
            <p>{{ pokemon.name }}</p>
            <a href="/pokemon/{{ forloop.index0 }}"
              ><img src="{{ pokemon.img }}" alt=""
            /></a>
            <form
              action="/pokemon/{{ forloop.index0 }}?_method=DELETE"
              method="POST"
            >
              <input type="submit" value="Delete" class="small" />
            </form>
            <form action="/pokemon/{{ forloop.index0 }}/edit">
              <input type="submit" value="Edit" class="small" />
            </form>
          </div>
        </li>
        <br />
        {% endfor %}
      </ul>
    </div>
  </body>
</html>
