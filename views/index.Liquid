<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1> Pokemons </h1>
    {% for every in data %}
    <div>
       
  <a href= "/{{ forloop.index0 }}" ><h2>{{ every.name }}</h2></a>
  <a href= "/{{ forloop.index0 }}" > <img src="{{ every.img }}" alt=""></a>
    </div>
    
     {% endfor %}
</body>
</html>


