<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokemon</title>
</head>

<body>
    <h1>{{ data.name }}</h1>
    <img src="{{ data.img }}" alt="{{ data.name }}"><br>
    <ul>
        Type(s):
        {% for t in data.type %}
        <li>{{ t }}</li>
        {% endfor %}<br>

        Stats:<br>
        HP: {{ data.stats.hp }}<br>
        Attack: {{ data.stats.attack }}<br>
        Defense: {{ data.stats.defense }}<br>
    </ul><br>
    <a href="/edit"><button>Edit Pokemon</button></a><br><br>
    <a href="/delete"><button>Delete Pokemon</button></a><br><br>
    <a href="/pokemon"><button>Pokedex Home</button></a><br><br>
</body>

</html>