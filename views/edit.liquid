<!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="/css/style.css">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
        <title>Edit</title>
    </head>
    <body>
        <h1>Edit page</h1>
        <div class="edit">
            <form action="/fruits/{{index}}?_method=PUT" method="POST">

                    {% comment %} The pokemon's name {% endcomment %}
                    Name: <input type="text" name="name" value="{{data.name}}">
                    {{data.name}}
                    <br>
                    {% comment %} The image of the pokemon {% endcomment %}
                    <img src="{{data.img}}" alt="" >
                    <br>
                    <ul>
                        {% comment %} An unordered list of the Pokemon's types (eg. water, poison, etc) {% endcomment %}
                        Type: 
                        {% comment %} for loop to run through and get all values {% endcomment %}
                        {{data.type}}
                        {% comment %} {% for pokemon in data %}
                        {{pokemon.type | split: "|" | join: ", " }}
                    
                        {%endfor%} <br> {% endcomment %}
                        {% comment %} The pokemon's stats for HP, ATTACK, and DEFENSE. {% endcomment %}
                        <br>
                        Stats: <br>
                        Hp: {{data.stats.hp}}, <br>
                            Attack: {{data.stats.attack}} <br>
                            Defense: {{data.stats.defense}} <br>
                            Speed: {{data.stats.speed}}
                        <br>
                    </ul>
            </form>
    </div>

        <a href="/pokemon/">Back to homepage</a> 


    </body>
    </html>