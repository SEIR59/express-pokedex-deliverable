<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta
			name="viewport"
			content="width=device-width, initial-scale=1.0"
		/>
		<link rel="stylesheet" href="/app.css" />
		<title>Edit Pokemon</title>
	</head>
	<body>
		<h1>Edit a Pokemon!</h1>

		<form action="/pokemon/{{ index }}?_method=PUT" method="POST">
			<label
				>Name:
				<input
					type="text"
					name="name"
					placeholder="{{ data.name }}"
				/>
			</label>

			<label
				>Image:
				<input type="text" 
                       name="img" 
                       placeholder="{{ data.img }}" />
			</label>

			<label
				>Type:
				<input
					type="text"
					name="type"
					placeholder="{{ data.type }}"
				/>
			</label>

			<label
				>HP
				<input
					type="text"
					name="hp"
					placeholder="{{ data.stats.hp }}"
				/>
			</label>

			<label
				>Attack
				<input
					type="text"
					name="attack"
					placeholder="{{ data.stats.attack }}"
				/>
			</label>

			<label
				>Defense
				<input
					type="text"
					name="defense"
					placeholder="{{ data.stats.defense }}"
				/>
			</label>
			<br />
			<br />
			<input type="submit" name="" value="Submit Changes" />
		</form>
	</body>
</html>
